version: '3.8'

services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
      - /app/node_modules  
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm start

  mock-api:
    build: ./mock-api
    ports:
      - "3001:3001"
